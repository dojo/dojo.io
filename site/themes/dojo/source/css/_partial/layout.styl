body {
	display: flex;
	min-height: 100vh;
	flex-direction: column;
	background-color: $color-background;
}

.content-container {
	contain();
}

.page-content {
	padding-top: $header-offset;
	flex: 1;
}

.outer {
	outer();
}

.inner {
	max-width: $max-width;
}

.row {
	display: flex;
}