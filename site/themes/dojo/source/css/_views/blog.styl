.body-archive,
.body-single-page {
	section {
		margin-left: auto;
		margin-right: auto;
		max-width: $content-max-width;
	}

	.full {
		full();
		max-width: 100vw;
	}

	.extend {
		extend();
		max-width: 100vw;
	}

}
